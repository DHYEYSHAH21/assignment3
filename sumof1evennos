#include <stdio.h>

int main() {
    int n;
    printf("Enter how many even numbers to sum: ");
    scanf("%d", &n);

    int sum = n * (n + 1);
    printf("Sum of first %d even numbers is: %d\n", n, sum);

    return 0;
}
